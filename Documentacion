Descripción
Este proyecto es una aplicación Flask que proporciona una API para obtener una lista de nombres de personas.
La API expone un endpoint que devuelve una lista de nombres en formato JSON.

Uso de la API
Ruta del Endpoint: /personas

Método HTTP: GET //Se utiliza para solicitar datos del servidor. En una solicitud GET, el cliente 
(como un navegador web o una herramienta de prueba como Postman)
solicita información específica del servidor. //

La ruta /personas devuelve una lista de nombres de personas en formato JSON.
